{
  "id": "allinone",
  "version": "1.0.1",
  "name": "Dev AIO",
  "options": {
    "enable_neovim": {
      "type": "boolean",
      "default": true,
      "description": "If support neovim"
    },
    "enable_zsh": {
      "type": "boolean",
      "default": true,
      "description": "If support zsh with oh-my-zsh and plugins"
    },
    "enable_tmux": {
      "type": "boolean",
      "default": true,
      "description": "If support tmux with plugins"
    },
    "github_auth_token": {
      "type": "string",
      "description": "github token set for copilot"
    },
    "create_user": {
      "type": "string",
      "default": "dev",
      "description": "If create user, this options can be 'ignore' or '[username]', default 'dev' (create a username 'dev')"
    },
    "user_uid": {
      "type": "string",
      "default": "1000",
      "description": "The user uid, ignore if create_user is 'ignore' or value is 'automatic'"
    },
    "user_gid": {
      "type": "string",
      "default": "1000",
      "description": "The user gid, ignore if create_user is 'ignore' or value is 'automatic'"
    }
  }
}
